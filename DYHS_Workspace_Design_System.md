# Dyhs Workspace 디자인 시스템

> v1.0 · 2026-09-24 · 원본: `MAKE_DYHS_Design_System(1).md` 및 첨부된 Dyhs Mail 3열 화면

## 1. 제품과 브랜드

**Dyhs Workspace**는 MAKE;가 운영하는 dyhs.kr 통합 작업 공간이다. `Dyhs Mail`, `Dyhs Auth`, `Dyhs URL`은 기존 제품명으로 유지한다. Workspace는 공통 탐색, 메일, 개인 설정과 관리 콘솔의 사용자 경험을 연결한다. 학교 공식 서비스로 오인되는 문구를 피하고 `운영 · MAKE;`를 명시한다.

- 헤더: 제공된 MAKE; 가로형 원본 로고 + 텍스트 `Dyhs Workspace`. 로고에 제품명을 합성하지 않는다.
- 메일 영역 제목은 `Dyhs Mail`, 로그인은 `Dyhs Auth`, URL 서비스는 `Dyhs URL`로 표시한다.
- 다크 배경에서도 검정 글자가 포함된 로고는 흰 카드에 담는다. 110×45 원본을 확대하지 않고 보이는 그림 둘레에 16px 이상 여백을 둔다.
- 실제 로고 파일이 리포지토리에 없다면 대체 로고를 그리지 말고 자산 확보를 작업 항목으로 남긴다.

## 2. 디자인 토큰

| 역할 | 라이트 | 다크 |
| --- | --- | --- |
| 브랜드 | `#34408D` | `#34408D` (큰 강조면) |
| 화면·카드 | `#FFFFFF` | `#000000` |
| 본문 | `#000000` | `#FFFFFF` |
| 보조 글자 | `rgba(0,0,0,.64)` | `rgba(255,255,255,.72)` |
| 구분선 | `rgba(0,0,0,.12)` | `rgba(255,255,255,.24)` |
| 선택 배경 | `rgba(52,64,141,.08)` | `rgba(255,255,255,.12)` |
| 주 버튼 | 브랜드 면/흰 글자 | 흰 면/검정 글자 |
| 초점 테두리 | 브랜드 2px | 흰색 2px |

새 브랜드 색을 추가하지 않는다. 오류·성공·읽지 않음은 색만으로 구분하지 않고 문구, 굵기, 아이콘을 함께 쓴다. 이미지 속 회청색 선택면은 브랜드 색의 투명도 합성으로 구현한다. 링크·텍스트·컨트롤은 실제 배경에서 WCAG AA 대비를 측정한다.

```css
:root {
  --brand: #34408D; --black: #000000; --white: #FFFFFF;
  --surface: var(--white); --text: var(--black);
  --muted: rgba(0,0,0,.64); --line: rgba(0,0,0,.12);
  --selected: rgba(52,64,141,.08); --focus: var(--brand);
  --font-ui: "Pretendard Variable", "Pretendard", sans-serif;
  --radius-control: 8px; --radius-card: 12px;
}
[data-theme="dark"] {
  --surface: var(--black); --text: var(--white);
  --muted: rgba(255,255,255,.72); --line: rgba(255,255,255,.24);
  --selected: rgba(255,255,255,.12); --focus: var(--white);
}
```

## 3. 글꼴·간격·컴포넌트

- UI 폰트는 자체 제공 Pretendard Variable. 발신자가 작성한 메일 본문에는 UI 글꼴이나 색을 강제하지 않는다.
- 제목 28/36px 700, 구역 20/28px 700, 카드 17/24px 600, 본문 15/24px 400, 버튼 15/22px 600, 보조 13/20px 400–500. 본문은 15px 이상.
- 간격 4/8/12/16/24/32/48/64px. 데스크톱 바깥 여백 32px, 태블릿 24px, 모바일 16px. 버튼·필드 반경 8px, 카드·대화상자 12px. 최소 조작 영역 44×44px.
- 필드에는 상시 라벨, 오류에는 원인과 다음 행동. 저장 중에는 중복 요청을 막고 `저장 중…`을 표시한다. 중요한 실패는 자동으로 사라지는 토스트로만 처리하지 않는다.
- 아이콘은 단색으로 통일하고 아이콘 단독 버튼에 접근 가능한 이름을 준다. 키보드 초점, 모달 초점 복귀, 화면 읽기 순서와 `prefers-reduced-motion`을 확인한다.

## 4. 공통 Workspace 셸

데스크톱은 **제품 탐색 레일 → 기능 탐색/메일 목록 → 상세 작업 영역**의 계층을 쓴다. 첨부 화면의 `MAKE; + Dyhs Mail` 좌측 폴더, 중앙 메일 목록, 우측 읽기 영역의 정보 밀도를 계승한다. Workspace 탐색을 추가할 때 좁은 뷰포트의 메일 읽기 폭을 빼앗지 않도록 탐색 레일을 접거나 아이콘 레일로 축약한다.

| 영역 | 내용 | 동작 |
| --- | --- | --- |
| 제품 레일 | MAKE; 로고, 홈, 메일, 캘린더, 주소록, URL, 설정, 관리자 메뉴 | 현재 위치 표시; 미구현 앱은 활성 링크로 노출하지 않음 |
| 기능 탐색 | 메일 폴더·새 메일, 설정 섹션, 관리 카테고리 | 페이지별 메뉴; 일관된 선택 상태 |
| 작업 영역 | 목록·읽기, 설정 폼, 관리 테이블 | URL로 위치 복원, 새로고침해도 선택 유지 |
| 계정 메뉴 | 계정 주소, 로그아웃, 보안 설정 | 모바일에서도 접근 가능 |

1280px 이상 메일에서는 제품 레일·폴더·메일 목록·읽기 영역을 표시한다. 768–1279px에서는 제품 레일을 축약하고 읽기 영역을 선택 시 전환한다. 767px 이하에서는 앱 → 폴더/목록 → 상세를 화면별로 전환하고 뒤로 가기 상태를 보존한다. 320px 폭에서 주요 업무에 가로 스크롤이 생기지 않게 한다. 임계값은 콘텐츠 검증에 따라 조정 가능하다.

## 5. 화면별 규칙

### 홈

최근 메일, 계정 상태, 저장 공간, 필요한 작업을 요약한다. 실제 데이터가 연결되지 않은 수치는 예시라고 분명히 표시한다. 캘린더·주소록은 구현 전까지 준비 중 카드 또는 숨김 처리한다.

### 메일

첨부 화면의 청색 헤더, 얇은 구분선, 선택된 메일의 연한 브랜드 면, 간결한 툴바를 유지한다. 폴더에서 받은편지함·보낸편지함·임시보관함·스팸·휴지통과 사용자 폴더를 표시한다. 목록은 발신자, 제목, 미리보기, 날짜, 읽음·첨부 상태를 보여준다. 읽기에서는 발신자·받는 사람·참조·첨부와 안전하게 격리된 본문을 표시한다. `새 메일`을 항상 찾을 수 있게 한다. 메일 본문에서 HTML/CSS가 Workspace 셸에 영향을 주지 않게 한다.

### 개인 설정

`일반`, `메일`, `계정 및 보안`, `저장 공간`으로 묶는다. 서명·자동 응답·전달·필터·별칭은 **실제 연동이 검증된 항목만 편집 가능**하게 한다. Authentik이 담당하는 비밀번호·패스키·추가 인증은 안정적인 지원 API가 확인되기 전까지 동일한 디자인의 설명과 안전한 Authentik 설정 딥링크로 연결한다. 외부 메일 앱의 앱 비밀번호는 웹 SSO와 구분한다.

### 관리 콘솔

권한이 확인된 사용자에게만 `관리`를 표시한다. 사용자, 메일함, 도메인, 별칭, 저장 공간을 업무 단위로 묶고, 각 화면에 현재 원본 시스템과 동기화 상태를 표시한다. 계정 삭제·권한 변경·도메인 변경은 대상과 영향을 명시한 확인 단계가 필요하다. 서버 측 권한 검사가 UI 노출 제어와 별도로 필수다.

## 6. 문구 및 빈 상태

작업 중심의 짧은 한국어를 쓴다: `메일 보내기`, `설정 저장`, `다시 시도`. `받은 메일이 없어요`, `메일함에 연결하지 못했어요. 잠시 후 다시 시도해 주세요.`처럼 상태와 행동을 함께 쓴다. 실제 전송을 하지 않는 시연에서는 화면 상단에 `미리보기 · 실제 메일을 보내거나 변경하지 않습니다`를 고정 표시한다.

## 7. 인수 기준

- [ ] MAKE; 원본과 제품 이름의 역할이 분리되고, 팔레트와 Pretendard가 적용된다.
- [ ] 첨부 이미지의 메일 목록·읽기 위계를 데스크톱에서 유지한다.
- [ ] 메일 읽기·쓰기, 설정, 관리 화면이 320px부터 사용 가능하다.
- [ ] 키보드 탐색, 초점, 대비, 44px 터치 영역, 오류 및 빈 상태를 점검한다.
- [ ] 데모 데이터와 실제 메일/설정 상태를 혼동할 수 없다.
